import { EditorTextoDirective } from './editor-texto.directive';

describe('EditorTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new EditorTextoDirective();
    expect(directive).toBeTruthy();
  });
});
